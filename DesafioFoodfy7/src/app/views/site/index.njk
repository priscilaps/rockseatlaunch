{% extends "layout.njk" %}

{% block content%}

    <section class="main-content">
        <div class="feature">
            <h1>As melhores receitas</h1>
            <p>Aprenda a construir os melhores pratos <br>
                com receitas criadas por profissionais <br>
                do mundo inteiro.
            </p>
        </div>
        <div class="popular-recipes">
            <h1>Mais acessadas</h1>
            <div class="recipes">
                {% for item in items %}
                    <div class="recipe" id="{{item.id}}">
                        <img src="{{item.image}}" alt="{{item.title}}" class="img-recipe">
                        <h2 class="title-recipe">{{item.title}}</h2>
                        <p class="author-recipe">por {{item.chef_name}}</p>
                    </div>
                {% endfor %}    
            </div>
        </div>
    </section>
    <script src="/scripts.js"></script>
{% endblock %}